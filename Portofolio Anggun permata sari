<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Portofolio | Nabila Maulida</title>
  <style>
    /* Font & Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: linear-gradient(135deg, #ffd6e0, #ffe6f0);
      color: #333;
      line-height: 1.6;
    }

    header {
      background: linear-gradient(90deg, #ffb6c1, #ffc1d6);
      text-align: center;
      padding: 2rem 1rem;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    header h1 {
      font-size: 2.5rem;
      color: #fff;
      animation: fadeInDown 1s ease;
    }

    header p {
      color: #fff;
      font-size: 1.1rem;
      animation: fadeInUp 1.2s ease;
    }

    section {
      max-width: 900px;
      margin: 2rem auto;
      padding: 2rem;
      background: #fff;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.08);
      animation: fadeIn 1
